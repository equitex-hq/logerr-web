/**
 * Allowed log environments.
 */
export const LOG_ENVIRONMENTS = ["production", "development"] as const;

/**
 * Allowed log levels.
 */
export const LOG_LEVELS = ["debug", "info", "warn", "error"] as const;
